import XCTest
import Quick
import Nimble
@testable import RizeppiDesktop

class AppTest: QuickSpec {
    override func spec() {
        describe("AppTest") {
            describe("smoke test") {
                it("should be able to run on launch") {

                }
            }
        }
    }
}
