import Quick
import Nimble
@testable import RizeppiMobile

class AppTest: QuickSpec {
    override func spec() {
        describe("AppTest") {
            describe("smoke test") {
                it("should be able to run on launch") {
                }
            }
        }
    }
}
